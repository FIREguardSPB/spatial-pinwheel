# FIX33

## Что исправлено

- Смягчено поведение SSE-переподключения на фронте.
- Убрана агрессивная реакция UI на кратковременные обрывы потока.
- Баннер и toast по потоку теперь показываются только при устойчивом обрыве, а не при каждом кратком `EventSource.onerror`.
- Убраны лишние массовые `invalidateQueries()` при каждом повторном открытии SSE.
- `ConnectionStatus` больше не тащит `useSignals()` на каждой странице как прокси для здоровья API.
- Страница сделок сохраняет предыдущие данные во время refetch и выглядит стабильнее.

## Цель

Убрать эффект "страница постоянно перезагружается" при периодических SSE-disconnect/reconnect.
