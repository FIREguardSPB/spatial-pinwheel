FIX52

Что изменено:
- Пресеты в настройках переведены в понятную модель:
  - черновик до сохранения,
  - сохранённый базовый шаблон,
  - сохранённый шаблон с ручными отклонениями,
  - ручная конфигурация.
- Выбор базового шаблона теперь сохраняется локально и не «теряется» при возврате на страницу.
- Добавлено явное пояснение, что пресеты влияют только на глобальные стартовые defaults, а не заменяют per-symbol adaptive plan.
- Добавлена кнопка «Считать ручной конфигурацией», чтобы убрать привязку к шаблону и снять путаницу.
- HelpLabel улучшен: теперь можно кликать по самому названию параметра, не только по иконке.
- Для дополнительных параметров добавлены tooltip + подробные модальные объяснения на русском языке.

Проверено:
- python -m compileall -q backend
- синтаксический прогон TS/TSX через typescript.transpileModule
