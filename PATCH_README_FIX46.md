FIX46

- removed frontend runtime dependency on recharts
- added lightweight internal SVG area chart component
- patched dashboard/account/backtest pages to use local chart component
- fixes Vite import-analysis crash: Failed to resolve import "recharts"
